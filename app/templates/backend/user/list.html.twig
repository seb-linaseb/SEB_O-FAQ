{% extends 'frontend_layout.html.twig' %}

{% block title %}USER LIST{% endblock %}

{% block content %}

    <h1>User index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Rôle</th>
                <th>Username</th>
                <th>Password</th>
                <th>Email</th>
                <th>Actif</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>

                <td>
                    {% for role in user.role %}
                        {{ role.name }}
                    {% else %}
                        Aucun rôle attribué
                    {% endfor %}
                </td>
                <td>
                    <a href="{{ path('user_show', {'username': user.username}) }}">
                    {{ user.username }}
                    </a>
                </td>
                <td>{{ user.password }}</td>
                <td>{{ user.email }}</td>
                <td>
                      {{ user.status ? 'Yes' : 'No' }}</td>
                </td>
                <td>
                    <a href="{{ path('user_show', {'username': user.username}) }}">show</a>
                    <a href="{#{{ path('backend_team_edit', {'id': team.id}) }}#}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{#{{ path('backend_team_new') }}#}">Create new</a>


{% endblock %}
