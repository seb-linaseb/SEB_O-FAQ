
 <nav class="navbar navbar-light  justify-content-between">

    <form class="form-inline" action="{#{{ path('movie_index') }}#}" method="POST">
        <input class="form-control mr-sm-2" type="search" name="title" placeholder="Search" aria-label="Search" minlength=3>

        <button class="btn btn-dark my-2 my-sm-0" type="submit">Search</button>
    </form>

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}{# est ce que mon utilisateur est authentifié ? #}
    
        <a class="btn btn-primary my-2 my-sm-0"  href="{{ path('backend_user_show', {'username': app.user.username}) }} ">
            {{ app.user.username }} - {{ app.user.role.name }}
        </a>

        <a class="btn btn-danger my-2 my-sm-0"  href="{{ path('app_logout')}} ">Déconnexion</a>
        
    {% else %} {# si non , alors je lui propose de se connecter#}
    
    <a class="btn btn-success my-2 my-sm-0"  href="{{ path('app_login')}} ">Connexion</a>

    {% endif %}

</nav>

